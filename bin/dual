#!/usr/bin/env ruby

def setup(option)
  `xrandr --output HDMI2 #{option} eDP1`
end

case ARGV[0]
when 'clone' then setup('--same-as')
when 'left' then setup('--left-of')
when 'right' then setup('--right-of')
else puts "Invalid options #{ARGV}"
end
