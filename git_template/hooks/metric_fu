#!/bin/sh

if [ -f Gemfile ] && [ -x $(which metric_fu) ] && [ -n "$METRIC_FU_OUT" ]; then
  echo "Running metric_fu"
  destination="${METRIC_FU_OUT}/$(basename $PWD)"
  mkdir -p $destination
  metric_fu --out $destination
fi
