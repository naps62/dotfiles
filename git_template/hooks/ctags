#!/bin/sh

set -e

echo "[hook] [ctags] Updating .git/tags"

PATH="/usr/local/bin:$PATH"
trap "rm -f .git/tags.$$" EXIT
ctags --tag-relative -Rf.git/tags.$$ --exclude=.git --languages=-javascript,sql > /dev/null 2>&1
mv .git/tags.$$ .git/tags
